<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.subik.myshoppinglist">

    <application
        android:name=".myapplication.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ListProductActivity"
            android:label="List Products"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EnterProductActivity"
            android:label="Enter Product" />
        <activity
            android:name=".EnterCouponActivity"
            android:label="Enter Coupon" />
        <activity
            android:name=".ListCouponActivity"
            android:label="List All Coupons" />
        <activity
            android:name=".ViewCouponActivity"
            android:label="View Coupon" />
        <activity
            android:name=".LargestDiscountActivity"
            android:label="Generate Largest Discount" />
        <activity android:name=".BestDiscountActivity" />
    </application>

</manifest>